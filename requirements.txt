# Legion Flask Web UI — Python dependencies
# Install: pip3 install --break-system-packages -r requirements.txt
#
# Tested on Kali Linux with Python 3.11+

# ── Web framework ──────────────────────────────────────────────────────────
flask>=3.0.0
werkzeug>=3.0.0          # Flask WSGI toolkit (also used directly for make_server)

# ── Database ───────────────────────────────────────────────────────────────
SQLAlchemy>=1.4,<3.0     # ORM; tested on 1.4.x and 2.0.x
six                       # unicode() shim used in db entities (from six import u as unicode)

# ── HTTP / networking ──────────────────────────────────────────────────────
requests>=2.28.0
urllib3>=2.0.0            # requests dependency; 2.x is compatible

# ── AI tab (Vertex AI / Anthropic) ────────────────────────────────────────
anthropic>=0.20.0         # AnthropicVertex client used in app/ai/analyzer.py
google-auth>=2.0.0        # ADC credentials required by anthropic[vertex]

# ── Shodan integration (scripts/python/pyShodan.py) ───────────────────────
pyShodan>=0.2.0           # Python Shodan API wrapper

# ── ExploitDB integration ──────────────────────────────────────────────────
pyExploitDb>=0.2.0        # searchsploit / exploitdb host action

# ── Terminal / startup UI ──────────────────────────────────────────────────
pyfiglet>=0.8.0
colorama>=0.4.0
termcolor>=2.0.0
rich>=13.0.0              # colour output in run_tests.sh and test suite

# ── Browser automation (Selenium tests + eyewitness screenshooter) ─────────
selenium>=4.9.0           # test suite and eyewitness Selenium module patching

# ── LeakSearch tool dependency ─────────────────────────────────────────────
neotermcolor>=2.0.0       # required by /opt/LeakSearch/LeakSearch.py

# ── Optional: Qt6 GUI mode (not needed for --web / Flask mode) ─────────────
# PyQt6
# qasync

# ── Development / testing only (not needed at runtime) ─────────────────────
# flake8
# pytest
# webdriver-manager
